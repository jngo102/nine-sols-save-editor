/** The types that a flag may be. */
export type FlagType = ObjectFlagFieldType | object;

/** The types that an object flag's fields may be. */
export type ObjectFlagFieldType = string | number | boolean;
